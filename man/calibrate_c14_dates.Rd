% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_c14_dates.R
\name{calibrate_c14_dates}
\alias{calibrate_c14_dates}
\title{Calibrate Radiocarbon Dates}
\usage{
calibrate_c14_dates(
  names,
  dates,
  standard_deviations,
  sigma = "two_sigma",
  plot = TRUE,
  output_plot_file = "Cal_c14_curve.png",
  output_csv_file = "Cal_c14_dates.csv"
)
}
\arguments{
\item{names}{A character vector of sample names.}

\item{dates}{A numeric vector of uncalibrated radiocarbon dates (BP).}

\item{standard_deviations}{A numeric vector of standard deviations for the dates.}

\item{sigma}{The sigma range to use for calibration (\code{"two_sigma"} by default).}

\item{plot}{Logical; if \code{TRUE}, generates a calibration plot.}

\item{output_plot_file}{Optional; file path to save the plot. If \code{NULL}, the plot is displayed but not saved.}

\item{output_csv_file}{Optional; file path to save the calibrated data as a CSV file. If \code{NULL}, no file is saved.}
}
\value{
A data frame containing the original dates and the calibrated date ranges in BP.
}
\description{
This function calibrates radiocarbon dates using the OxCal calibration curve via the \code{oxcAAR} package.
}
\details{
The function calibrates radiocarbon dates and converts the results to BP (Before Present) format. It can also generate a calibration plot and save the results to a CSV file.
}
\examples{
\dontrun{
# Example data
names <- c("Sample1", "Sample2")
dates <- c(1000, 1500)
standard_deviations <- c(50, 100)

# Calibrate dates
results <- calibrate_c14_dates(
  names = names,
  dates = dates,
  standard_deviations = standard_deviations,
  sigma = "two_sigma",
  plot = TRUE,
  output_plot_file = "calibrated_c14_dates_plot.png",
  output_csv_file = "calibrated_c14_data.csv"
)
}

}
